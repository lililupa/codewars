// DESCRIPTION:

// This code does not execute properly. Try to figure out why.
// function multiply(a, b){
//   a * b
// }

//SOLUTION:
function multiply(a, b) {
  let c = a * b;
  return c;
}
let mul = multiply(2, 2);
console.log(mul);
